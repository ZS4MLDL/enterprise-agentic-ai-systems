# Core requirements — works on Python 3.13, no C++ build tools needed
# Install now: pip install -r requirements.txt
#
# Module-specific extras (install when you reach that module):
#   M10/M11 vector store  →  pip install -r requirements-vector.txt
#   M13 observability     →  pip install -r requirements-observability.txt

# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
streamlit==1.40.0

# Pydantic (settings + validation)
pydantic==2.10.3
pydantic-settings==2.6.1

# LLM / AI  (M02+)
openai==1.57.0
langgraph==1.2.6
langgraph-checkpoint-sqlite==3.1.0
langchain-core==1.4.8

# Database  (M03+)
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0       # SQLite async (student mode)

# Queue / workers  (M09+)
redis==5.2.1
dramatiq[redis]==1.17.0

# Utilities
python-dotenv==1.0.1
httpx==0.28.1
tenacity==9.0.0

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
